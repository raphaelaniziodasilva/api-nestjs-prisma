/* eslint-disable prettier/prettier */
// vamos colacar regras na nossa aplicação ou niveis de permissão

export enum Role {
    // regras 
    User = 1,
    Admin = 2
}

// agora vamos criar um decorator va para a pasta de decorators e crie um arquivo roles.decorator.ts